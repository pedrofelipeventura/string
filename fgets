//USANDO FGETS NO LUGAR DE SCANF E GETS, POIS PARA /N  E ESPAÇO O PROGRAMA NÃO LÊ DIREITO A STRING DIGITADA
//LOGO, O USO DE FGETS É O MELHIOR, UTILIZANDOA  LIBRABY STDIN

#include <stdio.h>
#define TAM 50

//CODIGO DA ERRO DESSA FORM POR CAUSA QUE  ENTER È LIDO COMO CARACTERE
//GETCHAR RESOLVE OS PROBLEMAS

int main(){

    char str[TAM];

   // for(int k=0; k<TAM; k++){
   //     scanf("%c",&str[k]);// str[k] = getchar();
   //     getchar(); //RESOLVE OS PROBLEMAS DO CODIGO
   // }

    //scanf("%s",str); //DGITE A STRING MAS AINDA ESTÁ ERRADO POIS QUANDO ENCONTRA ENTER OU ESPAÇO ELE DÁ PAU

    fgets(str, TAM, stdin);

    puts("String:");
    //for(int k=0; k<TAM; k++){
    int k =0;
    while(str[k]!='\0'){
        printf("%c",str[k]);
        k++;
    }
    printf("\n");

    puts(str);

    return 0;
}
